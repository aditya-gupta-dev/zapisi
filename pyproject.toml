[project]
name = "zapisi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
    "gunicorn>=26.0.0",
    "libsql-client>=0.3.1",
    "python-dotenv>=1.2.2",
]
